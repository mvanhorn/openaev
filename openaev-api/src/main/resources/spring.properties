# jsqlparser is on the classpath for tenant SQL rewriting, which would otherwise switch
# Spring Data JPA to its jsqlparser-based native-query enhancer. That enhancer rejects some
# valid native queries (e.g. data-modifying CTEs), so keep the regex enhancer Spring Data
# used before jsqlparser was added.
spring.data.jpa.query.native.parser=regex
